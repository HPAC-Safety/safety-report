# Build output
bin/
obj/
*.user
*.suo
artifacts/
TestResults/
coverage/
*.coverage
*.cobertura.xml

# Local FileSystemBlobStore root (Development media storage stand-in)
.local/

# Editor/IDE
.idea/

# Node
node_modules/
.playwright/
playwright-report/
test-results/
src/web/dist/
tests/e2e/.features-gen/


# Installed and cached by `skillfile install` — reproduced from Skillfile.lock,
# never committed. See CONTRIBUTING.md.
.claude/skills/
.claude/agents/
.skillfile/cache/

# Written by `graphify claude install` — machine-local (hardcodes an absolute
# path to the graphify binary), never committed.
.claude/settings.json

# Secrets and local config
.env
.env.local
appsettings.Development.local.json

# OS
.DS_Store
Thumbs.db

# Graphify tooling and output are clone-local.
/graphify-out/
# Written by `./init-dev.sh --obsidian` from the graph — regenerated, never committed.
/obsidian-vault/
/.graphify-tool/
/.graphify-version
/.graphify-agent
/.gitattributes

# Agent-created git worktrees are clone-local, not repo content.
/.claude/worktrees/
